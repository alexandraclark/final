Guest
  user_id: integer      # if guest is a user
  first_name: string
  last_name: string
  email: string
  phone: integer

Invitation
  guest_id: integer
  event_id: integer
  RSVP: boolean
  attending: string

Event
  title: string
  date: integer
  details: text
  image_url: string
  budget: integer

List
  title: string
  event_id: integer

Task
  list_id: integer
  due_date: integer
  tag: string
  assigned_to: integer
  completed: boolean
  completed_date: integer

Supply
  event_id: integer
  title: string
  price: integer
  assigned_to: integer
  purchase_url: string
  purchased: boolean

User
  username: string
  email: string
  password: string
  guest_id: integer   # if user is a guest
